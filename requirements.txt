# 复现本仓库实验所需的 Python 依赖（Python 3.11）。
# 版本为实验实际运行的版本；torch / torchvision 按 CUDA 12.1 构建，
# 需从 PyTorch 官方索引安装：
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
# 纯 CPU 环境可去掉 +cu121 后缀（分析与可视化脚本不需要 GPU）。

torch==2.5.1+cu121
torchvision==0.20.1+cu121

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0

matplotlib==3.10.9
Pillow==12.2.0
PyYAML==6.0.3
